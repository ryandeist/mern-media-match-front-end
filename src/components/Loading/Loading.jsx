// imports
import styles from './Loading.module.css'

// component
const Loading = () => {

  // return
  return (
    <main className={styles.container}>
      <h1>Loading...</h1>
    </main>
  )
}

// export
export default Loading
